#include <stdio.h>
/**
 * main  - A program that prints
 * alphabets in reverse
 *
 * Return: 0 to be success
 */
int main(void)
{
char letter;
for (letter = 'z'; letter <= 'a'; z--)
{
putchar(letter);
}
putchar('\n');
return (0);
}
